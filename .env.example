# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
# Example: http://localhost:5173,https://your-project.web.app,https://yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Google Cloud Configuration
GCP_PROJECT_ID=your_gcp_project_id_here
GOOGLE_CLOUD_PROJECT=your_gcp_project_id_here
FIRESTORE_DATABASE_ID=your_database_id_here

# Firebase Storage Configuration
# Format: your-project-id.appspot.com or your custom bucket name
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Service Account (Required for local development)
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# Place the file in the server directory and update the path below
FIREBASE_SERVICE_ACCOUNT=./your-service-account.json

# Google OAuth Configuration (if using Google Auth)
# Get these from Google Cloud Console > APIs & Services > Credentials
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:5173/auth/google/callback

# JWT Configuration
# Generate a secure random string for JWT signing
# JWT_SECRET=your-super-secure-jwt-secret-here

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Admin Dashboard Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-secure-password-123
ADMIN_JWT_SECRET=super-secret-admin-jwt-key-change-in-production

# Database Configuration (when you add one)
# DATABASE_URL=postgresql://user:password@localhost:5432/hotlympics