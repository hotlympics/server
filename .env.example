# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
# Example: http://localhost:5173,https://your-project.web.app,https://yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Google Cloud Configuration
GCP_PROJECT_ID=your_gcp_project_id_here
FIRESTORE_DATABASE_ID=hotlympics

# For local development only (optional - use gcloud auth instead if possible)
# FIREBASE_SERVICE_ACCOUNT=/path/to/your/service-account-key.json

# Firebase Storage Configuration
# Format: your-project-id.appspot.com or your custom bucket name
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Database Configuration (when you add one)
# DATABASE_URL=postgresql://user:password@localhost:5432/hotlympics

# Firebase Admin SDK
# For production, use Application Default Credentials (gcloud auth)
# For local development, you can set this to point to a service account key file
# FIREBASE_SERVICE_ACCOUNT=/path/to/your/service-account-key.json

# Admin Dashboard Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-secure-password-123
ADMIN_JWT_SECRET=super-secret-admin-jwt-key-change-in-production